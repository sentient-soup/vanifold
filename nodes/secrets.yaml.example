# Copy to secrets.yaml (gitignored) and fill in.
wifi_ssid: "van-bench"
wifi_password: ""
# Hub Pi IP or hostname
mqtt_broker: "192.168.1.50"
# vanifold-node password, printed by scripts/install.sh on the Pi
# (or read /etc/vanifold/mqtt-credentials there)
mqtt_password: ""
ota_password: "pick-something"
